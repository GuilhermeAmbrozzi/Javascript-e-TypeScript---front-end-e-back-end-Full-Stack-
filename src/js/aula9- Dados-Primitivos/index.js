const nome = 'Luiz'; //string
const nome1 = "Raquel"; //string
const nome2 = `Samuel`; //string

const num = 10; //number inteiro = int
const num1 = 15.25; //number float

let nomeAluno; // undefined -> nã aponta para local nenhum na memória
let sobreNomeAluno = null; //Nulo -> não aponta pra local nenhum na memória
const aprovado = true; // boolean = true, false (valor lógico)

// Para ver o tipo de dado no Js usamos o typeof.
console.log(typeof nome, nome); // assim podemos ver o tipo e o valor da variável 
console.log(typeof num, num); // assim podemos ver o tipo e o valor da variável 
console.log(typeof num1, num1); // assim podemos ver o tipo e o valor da variável 
console.log(typeof nomeAluno, nomeAluno); // assim podemos ver o tipo e o valor da variável 
console.log(typeof aprovado, aprovado); // assim podemos ver o tipo e o valor da variável 
